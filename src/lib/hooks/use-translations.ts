'use client';

/**
 * Custom hooks for translations
 * Usage: const t = useTranslations('common')
 * Then: t('save'), t('cancel'), etc.
 */
export { useTranslations, useLocale } from '../i18n-context';

